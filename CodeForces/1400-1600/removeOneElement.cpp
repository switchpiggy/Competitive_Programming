#include <bits/stdc++.h>

using namespace std;

#define s(n) scanf("%d", &n);

int main() {

    int t;

    int arr[20005];

    s(t);

    for(int i = 0; i < t; ++i) s(arr[i]);

    for(int i = 0; i < t; ++i) {

        

    }

}