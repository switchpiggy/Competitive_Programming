#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;
ll n, m, a[100007], maxn[100007], minn[100007], row[100007];
map<ll, ll> pos;

int main() {
    cin >> n >> m;
    for(ll i = 1; i <= n; ++i) cin >> a[i];
}