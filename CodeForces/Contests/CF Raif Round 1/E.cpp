#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;
ll n, k, a[100007];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> k;
    for(ll i = 0; i <  n; ++i) cin >> a[i];

    
}