#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;
ll t, a, b, c, d;

int main() {
    cin >> t;
    while(t--) {
        cin >> a >> b >> c >> d;
        cout << a << ' ' << c << ' ' << c << endl;
    }

    return 0;
}