#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;
ll n, a[100007],b[100007];
string s, t;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> s;
    for(ll i = 0; i < n; ++i) cin >> a[i];
}