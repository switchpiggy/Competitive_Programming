#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;
ll x, t;

int main() {
    cin >> t;
    while(t--) {
        cin >> x;
        cout << 1 << ' ' << x - 1 << endl;
    }

    return 0;
}