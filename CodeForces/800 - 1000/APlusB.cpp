#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;

int main() {
    ll t, a, b;
    scanf("%lld", &t);

    while(t--) {
        scanf("%lld %lld", &a, &b);
        printf("%lld\n", a + b);
    }
}